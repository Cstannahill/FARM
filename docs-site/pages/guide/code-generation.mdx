# Code Generation Pipeline

FARM automatically converts Pydantic models and FastAPI routes into TypeScript interfaces and client functions. React hooks are generated for AI operations and API calls.

The pipeline runs during development and on builds, ensuring frontend and backend stay type aligned.
